name: monorepo

environment:
  sdk: ">=3.0.0 <4.0.0"
dev_dependencies:
  build_runner: ^2.4.9
  freezed: ^2.5.2
  melos: ^6.2.0
  starklints:
    path: packages/starklints

dependencies:
  starknet_provider: ^0.1.0
