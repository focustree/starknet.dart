import 'package:flutter/material.dart';

class ThirdPartyProviderCell extends StatelessWidget {
  final String name;
  final String logoAssetPath;
  final String url;
  const ThirdPartyProviderCell({
    Key? key,
    required this.name,
    required this.logoAssetPath,
    required this.url,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
