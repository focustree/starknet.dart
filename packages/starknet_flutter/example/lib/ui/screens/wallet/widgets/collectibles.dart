import 'package:flutter/material.dart';

class Collectibles extends StatelessWidget {
  const Collectibles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Text("Collectibles"),
          Text("Coming soon!"),
        ],
      ),
    );
  }
}
