name: ark
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0" # Dart version constraint

dependencies:
  starknet: ^0.1.0
  starknet_provider: ^1.0.0
  http: ^1.2.2
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.13
  dotenv: ^4.2.0
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  lints: ^5.0.0
  test: ^1.25.8
