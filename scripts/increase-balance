# starkli invoke $BALANCE_CONTRACT_ADDRESS increase_balance $1 --private-key $STARKNET_PRIVATE_KEY
dart run examples/starknet_cli/bin/sn.dart invoke -a $BALANCE_CONTRACT_ADDRESS -s increase_balance -c $1